import Trawler from '../components/Trawler';

/**
 * Standalone Trawler page
 * Wallet cleanup tool - closes empty token accounts to reclaim SOL
 * Access at: /trawler
 */
export default function TrawlerPage() {
  return <Trawler />;
}
